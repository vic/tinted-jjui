# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: Brush Trees Dark
# Scheme author: Abraham White &lt;abelincoln.white@gmail.com&gt;
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#b0c5c8", bg = "#485867" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#8299a1" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#6d828e", fg = "#b0c5c8" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#5a6d7a" }                              # Lighter background (base01)
"title"                          = { fg = "#868cb3", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#b386b2" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#aab386", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#87b386", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#b38686", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#86b3b3", fg = "#485867" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#87b386", fg = "#485867" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#5a6d7a" }                              # Lighter background (base01)
"status title"                   = { fg = "#485867", bg = "#868cb3", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#b386b2" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#8299a1" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#868cb3", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#b0c5c8", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#b0c5c8" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#6d828e", fg = "#b0c5c8" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#aab386", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#8299a1" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#b0c5c8" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#8299a1" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#6d828e" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#b0c5c8" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#6d828e", fg = "#b0c5c8", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#485867" }                              # Default background (base00)
"help title"                     = { fg = "#87b386", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#5a6d7a" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#485867" }                              # Default background (base00)
"menu title"                     = { fg = "#485867", bg = "#b386b2", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#b386b2" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#aab386", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#8299a1" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#5a6d7a" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#6d828e", fg = "#b0c5c8" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#485867" }                              # Default background (base00)
"confirmation text"              = { fg = "#868cb3", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#6d828e", fg = "#b0c5c8" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#8299a1" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#b38686", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#485867" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#8299a1" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#6d828e", fg = "#b0c5c8" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#b0c5c8" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#5a6d7a" }                              # Lighter background (base01)
