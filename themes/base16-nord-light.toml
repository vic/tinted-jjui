# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: Nord Light
# Scheme author: threddast, based on fuxialexander&#39;s doom-nord-light-theme (Doom Emacs)
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#2e3440", bg = "#e5e9f0" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#aebacf" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#b8c5db", fg = "#2e3440" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#c2d0e7" }                              # Lighter background (base01)
"title"                          = { fg = "#3b6ea8", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#97365b" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#9a7500", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#4f894c", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#99324b", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#398eac", fg = "#e5e9f0" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#4f894c", fg = "#e5e9f0" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#c2d0e7" }                              # Lighter background (base01)
"status title"                   = { fg = "#e5e9f0", bg = "#3b6ea8", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#97365b" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#aebacf" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#3b6ea8", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#2e3440", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#2e3440" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#b8c5db", fg = "#2e3440" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#9a7500", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#aebacf" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#2e3440" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#aebacf" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#b8c5db" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#2e3440" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#b8c5db", fg = "#2e3440", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#e5e9f0" }                              # Default background (base00)
"help title"                     = { fg = "#4f894c", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#c2d0e7" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#e5e9f0" }                              # Default background (base00)
"menu title"                     = { fg = "#e5e9f0", bg = "#97365b", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#97365b" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#9a7500", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#aebacf" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#c2d0e7" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#b8c5db", fg = "#2e3440" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#e5e9f0" }                              # Default background (base00)
"confirmation text"              = { fg = "#3b6ea8", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#b8c5db", fg = "#2e3440" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#aebacf" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#99324b", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#e5e9f0" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#aebacf" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#b8c5db", fg = "#2e3440" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#2e3440" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#c2d0e7" }                              # Lighter background (base01)
