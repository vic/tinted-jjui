# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: Unikitty Light
# Scheme author: Josh W Lewis (@joshwlewis)
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#6c696e", bg = "#ffffff" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#a7a5a8" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#c4c3c5", fg = "#6c696e" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#e1e1e2" }                              # Lighter background (base01)
"title"                          = { fg = "#775dff", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#aa17e6" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#dc8a0e", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#17ad98", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#d8137f", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#149bda", fg = "#ffffff" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#17ad98", fg = "#ffffff" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#e1e1e2" }                              # Lighter background (base01)
"status title"                   = { fg = "#ffffff", bg = "#775dff", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#aa17e6" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#a7a5a8" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#775dff", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#6c696e", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#6c696e" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#c4c3c5", fg = "#6c696e" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#dc8a0e", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#a7a5a8" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#6c696e" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#a7a5a8" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#c4c3c5" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#6c696e" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#c4c3c5", fg = "#6c696e", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#ffffff" }                              # Default background (base00)
"help title"                     = { fg = "#17ad98", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#e1e1e2" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#ffffff" }                              # Default background (base00)
"menu title"                     = { fg = "#ffffff", bg = "#aa17e6", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#aa17e6" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#dc8a0e", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#a7a5a8" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#e1e1e2" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#c4c3c5", fg = "#6c696e" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#ffffff" }                              # Default background (base00)
"confirmation text"              = { fg = "#775dff", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#c4c3c5", fg = "#6c696e" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#a7a5a8" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#d8137f", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#ffffff" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#a7a5a8" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#c4c3c5", fg = "#6c696e" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#6c696e" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#e1e1e2" }                              # Lighter background (base01)
