# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: standardized-light
# Scheme author: ali (https://github.com/ali-githb/base16-standardized-scheme)
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#444444", bg = "#ffffff" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#767676" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#cccccc", fg = "#444444" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#eeeeee" }                              # Lighter background (base01)
"title"                          = { fg = "#3173c5", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#9e57c2" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#ad8200", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#31861f", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#d03e3e", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#00998f", fg = "#ffffff" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#31861f", fg = "#ffffff" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#eeeeee" }                              # Lighter background (base01)
"status title"                   = { fg = "#ffffff", bg = "#3173c5", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#9e57c2" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#767676" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#3173c5", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#444444", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#444444" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#cccccc", fg = "#444444" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#ad8200", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#767676" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#444444" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#767676" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#cccccc" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#444444" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#cccccc", fg = "#444444", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#ffffff" }                              # Default background (base00)
"help title"                     = { fg = "#31861f", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#eeeeee" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#ffffff" }                              # Default background (base00)
"menu title"                     = { fg = "#ffffff", bg = "#9e57c2", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#9e57c2" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#ad8200", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#767676" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#eeeeee" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#cccccc", fg = "#444444" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#ffffff" }                              # Default background (base00)
"confirmation text"              = { fg = "#3173c5", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#cccccc", fg = "#444444" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#767676" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#d03e3e", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#ffffff" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#767676" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#cccccc", fg = "#444444" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#444444" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#eeeeee" }                              # Lighter background (base01)
