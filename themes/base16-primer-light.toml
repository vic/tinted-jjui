# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: Primer Light
# Scheme author: Jimmy Lin
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#2f363d", bg = "#fafbfc" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#959da5" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#d1d5da", fg = "#2f363d" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#e1e4e8" }                              # Lighter background (base01)
"title"                          = { fg = "#0366d6", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#ea4aaa" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#ffd33d", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#28a745", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#d73a49", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#79b8ff", fg = "#fafbfc" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#28a745", fg = "#fafbfc" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#e1e4e8" }                              # Lighter background (base01)
"status title"                   = { fg = "#fafbfc", bg = "#0366d6", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#ea4aaa" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#959da5" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#0366d6", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#2f363d", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#2f363d" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#d1d5da", fg = "#2f363d" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#ffd33d", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#959da5" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#2f363d" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#959da5" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#d1d5da" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#2f363d" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#d1d5da", fg = "#2f363d", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#fafbfc" }                              # Default background (base00)
"help title"                     = { fg = "#28a745", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#e1e4e8" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#fafbfc" }                              # Default background (base00)
"menu title"                     = { fg = "#fafbfc", bg = "#ea4aaa", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#ea4aaa" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#ffd33d", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#959da5" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#e1e4e8" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#d1d5da", fg = "#2f363d" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#fafbfc" }                              # Default background (base00)
"confirmation text"              = { fg = "#0366d6", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#d1d5da", fg = "#2f363d" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#959da5" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#d73a49", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#fafbfc" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#959da5" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#d1d5da", fg = "#2f363d" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#2f363d" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#e1e4e8" }                              # Lighter background (base01)
