# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: PaperColor Dark
# Scheme author: Jon Leopard (http://github.com/jonleopard), based on PaperColor Theme (https://github.com/NLKNguyen/papercolor-theme)
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#808080", bg = "#1c1c1c" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#d7af5f" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#5faf00", fg = "#808080" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#af005f" }                              # Lighter background (base01)
"title"                          = { fg = "#ff5faf", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#00afaf" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#afd700", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#af87d7", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#585858", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#ffaf00", fg = "#1c1c1c" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#af87d7", fg = "#1c1c1c" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#af005f" }                              # Lighter background (base01)
"status title"                   = { fg = "#1c1c1c", bg = "#ff5faf", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#00afaf" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#d7af5f" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#ff5faf", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#808080", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#808080" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#5faf00", fg = "#808080" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#afd700", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#d7af5f" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#808080" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#d7af5f" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#5faf00" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#808080" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#5faf00", fg = "#808080", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#1c1c1c" }                              # Default background (base00)
"help title"                     = { fg = "#af87d7", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#af005f" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#1c1c1c" }                              # Default background (base00)
"menu title"                     = { fg = "#1c1c1c", bg = "#00afaf", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#00afaf" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#afd700", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#d7af5f" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#af005f" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#5faf00", fg = "#808080" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#1c1c1c" }                              # Default background (base00)
"confirmation text"              = { fg = "#ff5faf", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#5faf00", fg = "#808080" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#d7af5f" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#585858", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#1c1c1c" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#d7af5f" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#5faf00", fg = "#808080" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#808080" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#af005f" }                              # Lighter background (base01)
