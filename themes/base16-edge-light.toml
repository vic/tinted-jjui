# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: Edge Light
# Scheme author: cjayross (https://github.com/cjayross)
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#5e646f", bg = "#fafafa" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#5e646f" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#d69822", fg = "#5e646f" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#7c9f4b" }                              # Lighter background (base01)
"title"                          = { fg = "#6587bf", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#b870ce" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#d69822", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#7c9f4b", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#db7070", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#509c93", fg = "#fafafa" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#7c9f4b", fg = "#fafafa" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#7c9f4b" }                              # Lighter background (base01)
"status title"                   = { fg = "#fafafa", bg = "#6587bf", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#b870ce" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#5e646f" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#6587bf", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#5e646f", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#5e646f" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#d69822", fg = "#5e646f" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#d69822", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#5e646f" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#5e646f" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#5e646f" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#d69822" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#5e646f" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#d69822", fg = "#5e646f", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#fafafa" }                              # Default background (base00)
"help title"                     = { fg = "#7c9f4b", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#7c9f4b" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#fafafa" }                              # Default background (base00)
"menu title"                     = { fg = "#fafafa", bg = "#b870ce", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#b870ce" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#d69822", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#5e646f" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#7c9f4b" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#d69822", fg = "#5e646f" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#fafafa" }                              # Default background (base00)
"confirmation text"              = { fg = "#6587bf", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#d69822", fg = "#5e646f" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#5e646f" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#db7070", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#fafafa" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#5e646f" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#d69822", fg = "#5e646f" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#5e646f" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#7c9f4b" }                              # Lighter background (base01)
