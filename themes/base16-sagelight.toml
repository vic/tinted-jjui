# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: Sagelight
# Scheme author: Carter Veldhuizen
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#383838", bg = "#f8f8f8" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#b8b8b8" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#d8d8d8", fg = "#383838" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#e8e8e8" }                              # Lighter background (base01)
"title"                          = { fg = "#a0a7d2", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#c8a0d2" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#ffdc61", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#a0d2c8", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#fa8480", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#a2d6f5", fg = "#f8f8f8" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#a0d2c8", fg = "#f8f8f8" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#e8e8e8" }                              # Lighter background (base01)
"status title"                   = { fg = "#f8f8f8", bg = "#a0a7d2", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#c8a0d2" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#b8b8b8" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#a0a7d2", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#383838", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#383838" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#d8d8d8", fg = "#383838" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#ffdc61", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#b8b8b8" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#383838" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#b8b8b8" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#d8d8d8" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#383838" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#d8d8d8", fg = "#383838", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#f8f8f8" }                              # Default background (base00)
"help title"                     = { fg = "#a0d2c8", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#e8e8e8" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#f8f8f8" }                              # Default background (base00)
"menu title"                     = { fg = "#f8f8f8", bg = "#c8a0d2", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#c8a0d2" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#ffdc61", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#b8b8b8" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#e8e8e8" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#d8d8d8", fg = "#383838" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#f8f8f8" }                              # Default background (base00)
"confirmation text"              = { fg = "#a0a7d2", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#d8d8d8", fg = "#383838" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#b8b8b8" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#fa8480", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#f8f8f8" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#b8b8b8" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#d8d8d8", fg = "#383838" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#383838" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#e8e8e8" }                              # Lighter background (base01)
