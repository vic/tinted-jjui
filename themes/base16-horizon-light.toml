# tinted-jjui (https://github.com/vic/tinted-jjui)
# Scheme name: Horizon Light
# Scheme author: Michaël Ball (http://github.com/michael-ball/)
# Template author: Victor Borja <vborja@apache.org> (https://github.com/vic)

# Base16 Styling Guide: https://github.com/tinted-theming/home/blob/main/styling.md
# jjui Theme Documentation: https://github.com/idursun/jjui/wiki/Themes
# jjui default light theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_light.toml
# jjui default dark theme: https://github.com/idursun/jjui/blob/main/internal/config/default/default_dark.toml
# tinted gallery: https://tinted-theming.github.io/tinted-gallery/

# Selector                       = Color Spec                                              # Base16 styling.

# Global UI Elements
# ------------------
"text"                           = { fg = "#403c3d", bg = "#fdf0ed" }      # Default foreground, caret, delimiters (base05); default background (base00)
"dimmed"                         = { fg = "#bdb3b1" }                              # Comments, invisibles, line highlighting (base03)
"selected"                       = { bg = "#f9cbbe", fg = "#403c3d" }      # Selection background (base02); default foreground (base05)
"border"                         = { fg = "#fadad1" }                              # Lighter background (base01)
"title"                          = { fg = "#da103f", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"shortcut"                       = { fg = "#1d8991" }                              # Keywords, storage, selector, markup italic (base0E)
"matched"                        = { fg = "#fbe0d9", underline = true }            # Classes, markup bold, search text background (base0A)
"success"                        = { fg = "#94e1b0", bold = true }                 # Strings, inherited class, markup code, diff inserted (base0B) - emphasize positive states
"error"                          = { fg = "#f7939b", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize error states

# Operation Markers
# -----------------
"source_marker"                  = { bg = "#dc3318", fg = "#fdf0ed" }      # Support, regular expressions, escape characters, markup quotes (base0C); high contrast with main background
"target_marker"                  = { bg = "#94e1b0", fg = "#fdf0ed" }      # Strings, inherited class, markup code, diff inserted (base0B); high contrast with main background
"revisions rebase source_marker" = { bold = true }                                         # Source marker in rebase (bold)
"revisions rebase target_marker" = { bold = true }                                         # Target marker in rebase (bold)

# Status Bar
# ----------
"status"                         = { bg = "#fadad1" }                              # Lighter background (base01)
"status title"                   = { fg = "#fdf0ed", bg = "#da103f", bold = true } # Functions, methods, attribute IDs, headings (base0D)
"status shortcut"                = { fg = "#1d8991" }                              # Keywords, storage, selector, markup italic (base0E)
"status dimmed"                  = { fg = "#bdb3b1" }                              # Comments, invisibles (base03)

# Revset (Top Bar)
# ----------------
"revset title"                   = { fg = "#da103f", bold = true }                 # Functions, methods, attribute IDs, headings (base0D)
"revset text"                    = { fg = "#403c3d", bold = true }                 # User input area with default foreground (base05), recommended bold
"revset completion text"         = { fg = "#403c3d" }                              # Default foreground, caret, delimiters (base05)
"revset completion selected"     = { bg = "#f9cbbe", fg = "#403c3d" }      # Selection background (base02); default foreground (base05)
"revset completion matched"      = { fg = "#fbe0d9", bold = true }                 # Classes, markup bold, search text background (base0A)
"revset completion dimmed"       = { fg = "#bdb3b1" }                              # Comments, invisibles (base03)

# Revisions & Logs
# ----------------
"revisions"                      = { fg = "#403c3d" }                              # Default foreground, caret, delimiters (base05)
"revisions selected"             = { bold = true }                                         # Highlighted line in revisions/oplog (bold)
"revisions dimmed"               = { fg = "#bdb3b1" }                              # Comments, invisibles (base03)
"revisions details selected"     = { bg = "#f9cbbe" }                              # Selection background (base02)
"oplog selected"                 = { bold = true }                                         # Highlighted line in oplog (bold)
"evolog"                         = { fg = "#403c3d" }                              # Default foreground, caret, delimiters (base05)
"evolog selected"                = { bg = "#f9cbbe", fg = "#403c3d", bold = true } # Selection background (base02); default foreground (base05)

# Help Window
# -----------
"help"                           = { bg = "#fdf0ed" }                              # Default background (base00)
"help title"                     = { fg = "#94e1b0", bold = true, underline = true } # Strings, inherited class, markup code, diff inserted (base0B) - differentiate from other titles
"help border"                    = { fg = "#fadad1" }                              # Lighter background (base01)

# Menus
# -----
"menu"                           = { bg = "#fdf0ed" }                              # Default background (base00)
"menu title"                     = { fg = "#fdf0ed", bg = "#1d8991", bold = true } # Keywords, storage, selector (base0E) background for visibility; high contrast text
"menu shortcut"                  = { fg = "#1d8991" }                              # Keywords, storage, selector, markup italic (base0E)
"menu matched"                   = { fg = "#fbe0d9", bold = true }                 # Classes, markup bold, search text background (base0A)
"menu dimmed"                    = { fg = "#bdb3b1" }                              # Comments, invisibles (base03)
"menu border"                    = { fg = "#fadad1" }                              # Lighter background (base01)
"menu selected"                  = { bg = "#f9cbbe", fg = "#403c3d" }      # Selection background (base02); default foreground (base05)

# Confirmation Dialogs
# --------------------
"confirmation"                   = { bg = "#fdf0ed" }                              # Default background (base00)
"confirmation text"              = { fg = "#da103f", bold = true }                 # Functions, methods, attribute IDs, headings (base0D) - important dialog text
"confirmation selected"          = { bg = "#f9cbbe", fg = "#403c3d" }      # Selection background (base02); default foreground (base05)
"confirmation dimmed"            = { fg = "#bdb3b1" }                              # Comments, invisibles (base03)
"confirmation border"            = { fg = "#f7939b", bold = true }                 # Variables, XML tags, markup link text, markup lists, diff deleted (base08) - emphasize warning borders

# Undo Operations
# ---------------
"undo"                           = { bg = "#fdf0ed" }                              # Default background (base00)
"undo confirmation dimmed"       = { fg = "#bdb3b1" }                              # Comments, invisibles (base03)
"undo confirmation selected"     = { bg = "#f9cbbe", fg = "#403c3d" }      # Selection background (base02); default foreground (base05)

# Preview Pane
# ------------
"preview"                        = { fg = "#403c3d" }                              # Default foreground, caret, delimiters (base05)
"preview border"                 = { fg = "#fadad1" }                              # Lighter background (base01)
